//import {Address} from './address'; se tivesse optado por um objeto endereco

export class Person {
    id: number;
    name: string;
    creditLimit: number;
    risk: string;
    shortAddress: string;
    rate: number;
   //address: Address = new Address(); se tivesse optado por um objeto endereco
 }